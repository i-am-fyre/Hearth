# =============================================================================
# Hearth Frontend Configuration
# Copy this file to .env if deploying to a custom host.
# For local development with docker-compose, no changes are needed.
# =============================================================================

# Backend API URL — only set if your backend is not on localhost
# VITE_API_URL=http://your-server.com/api/v1
